services:
  uvend-app:
    image: uvend/uvend-nuxt:main
    ports:
      - ${PORT:-3002}:3002
    environment:
      - NODE_ENV=production
      - PORT=${PORT:-3002}
    command: ["node", ".output/server/index.mjs"]
    restart: unless-stopped
    env_file:
      - .env
networks: {}
